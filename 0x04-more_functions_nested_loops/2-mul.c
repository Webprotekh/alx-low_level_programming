#include "main.h"

/**
 * mul - Function to pritn the multiplication of two numbers
 * @a: The first Parameter to check
 * @b: The second parameter to checkh
 *
 * Return: The result of mul
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
