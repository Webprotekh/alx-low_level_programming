#include "main.h"

/**
 * islower - Checking for lower case
 *
 * Return: Always 1 or 0
 */

int _islower(int c)
{
	int letter = 'e';

	if (_islower(letter))
	{
		return (1);;
	}
	else
		return (0);
}
