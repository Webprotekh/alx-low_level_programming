#include "main.h"

/**
 * _abs - printing the absolute numbers of an integer
 *
 * @i: - parameter
 *
 * Return: Always 0
 */

int _abs(int i)
{
	if (r < 0)
	{
		r = (-1) * r;
		_putchar(r);
	}
	else
		_putchar(r);
	return (0);
}
