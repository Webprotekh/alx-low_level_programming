#include "main.h"

/**
 * _abs - printing the absolute numbers of an integer
 *
 * @i: - parameter
 *
 * Return: Always 0
 */

int _abs(int i)
{
	int num = i;

	if (num > 0)
	{
		_putchar(r);
		return (num)
	}
	else if (num < 0)
	{
		_putchar(r);
		return (-num);
	}
	else
		return (0);
}
